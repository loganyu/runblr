
<li>  <%= link_to comment.body, link_comment_url(comment.link.id, comment.id) %> </li>
  <ul>
    <% comments_by_parent[comment.id].each do |child_comment| %>
      <li><%= comment_partial(child_comment, comments_by_parent) %></li>
    <% end %>
  </ul>